# Renders + caption sidecars never belong in the repo (they go to the vault /
# Drive). Keep the engine, schema, and manifests tracked; ignore outputs.
*.mp4
*.mov
*.srt
# Auto-generated orchestrated edit manifests (build_video.py rewrites them each
# assemble run from the script + image manifest + VO kit). Regenerable output.
manifests/*_orchestrated_*.json
manifests/*.bak-*
