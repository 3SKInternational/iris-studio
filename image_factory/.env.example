# Copy to .env (same dir) and fill in. .env is gitignored — never commit the key.
#   cp .env.example .env && chmod 600 .env
# The vault is git-tracked + synced, so the key lives HERE, not in the vault.
OPENAI_API_KEY=sk-...
